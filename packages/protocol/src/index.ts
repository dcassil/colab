/**
 * @colab/protocol — public entry (skeleton).
 *
 * Neutral placeholder. Domain types (ScopeId, envelopes, event constants)
 * are introduced by initiative I2 (Protocol & Core). No logic lives here yet.
 */

/** Package name marker, replaced by real protocol exports in I2. */
export const PROTOCOL_PACKAGE = "@colab/protocol" as const;
