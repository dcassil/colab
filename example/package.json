{
  "name": "example",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "colab example app (I7) — one-line multiplayer: live cursors, roster, advisory edit-lock, and a custom interaction. No iframe, no CMS, no geometry.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "e2e": "playwright test"
  },
  "dependencies": {
    "colab-ui": "workspace:*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7",
    "vitest": "^3.2.7"
  }
}
